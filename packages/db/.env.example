# prisma.config.ts does `import "dotenv/config"`, which resolves from this
# package's directory rather than the repo root, so this package needs its
# own copy of DATABASE_URL even when the repo root already has one.
DATABASE_URL=postgresql://collabtime:collabtime123@localhost:5433/collabtime
