# =============================================================================
# Collabtime Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.
# Required variables are marked with [REQUIRED].
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PlanetScale/MySQL) [REQUIRED]
# -----------------------------------------------------------------------------
# Get this from your PlanetScale dashboard: https://planetscale.com/
DATABASE_URL=mysql://user:password@host/database?sslaccept=strict

# -----------------------------------------------------------------------------
# Upstash Redis [REQUIRED]
# -----------------------------------------------------------------------------
# Get these from your Upstash Redis dashboard: https://console.upstash.com/
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-token

# -----------------------------------------------------------------------------
# Better Auth [REQUIRED]
# -----------------------------------------------------------------------------
# Generate a random secret (min 32 chars): openssl rand -base64 32
BETTER_AUTH_SECRET=your-super-secret-key-here-minimum-32-chars
# The URL where your app is hosted
BETTER_AUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Stripe [REQUIRED]
# -----------------------------------------------------------------------------
# Get these from your Stripe dashboard: https://dashboard.stripe.com/
# Secret key (starts with sk_test_ or sk_live_)
STRIPE_SECRET_KEY=sk_test_your_key_here
# Webhook secret (starts with whsec_) - create webhook at /api/auth/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
# Price ID for the PRO plan (starts with price_)
STRIPE_PRO_PRICE_ID=price_your_pro_price_id

# -----------------------------------------------------------------------------
# Optional: Space Access Token Signing
# -----------------------------------------------------------------------------
# If not set, falls back to BETTER_AUTH_SECRET
# Generate with: openssl rand -base64 32
# SPACE_ACCESS_SECRET=your-space-access-secret-here

# -----------------------------------------------------------------------------
# Optional: Resend Email
# -----------------------------------------------------------------------------
# Get these from your Resend dashboard: https://resend.com/
# RESEND_API_KEY=re_your_api_key
# RESEND_FROM_EMAIL=noreply@yourdomain.com

# -----------------------------------------------------------------------------
# Web App URL (for redirects)
# -----------------------------------------------------------------------------
# Optional in development, should be set in production
WEB_APP_URL=http://localhost:3000
