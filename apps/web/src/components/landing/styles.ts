// The design system's `lg` button is h-9, sized for application UI. Marketing CTAs
// need to clear the 44px mobile touch target and read at text-base on small screens,
// so both landing CTAs share this override.
const LANDING_CTA_SIZE = "h-12 w-full text-base sm:h-14 sm:w-auto sm:min-w-64 sm:text-lg";

export { LANDING_CTA_SIZE };
